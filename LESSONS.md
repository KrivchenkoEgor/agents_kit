# LESSONS.md — Журнал уроков

Этот файл агент заполняет автоматически после успешных задач.

## Правила
- Записывать только **новые** наблюдения: особенность данных, рабочий приём, найденные грабли
- Рутина (прочитал файл, посчитал строки) не логируется
- Уроки, которые повторяются или доказали ценность, предлагаются к закреплению в `AGENTS.md` — добавляются только с одобрения пользователя

## Формат урока

### [YYYY-MM-DD HH:MM] Название задачи
**Тип задачи:** (анализ / очистка / график / модель / прочее)
**Что сделано:** кратко
**Особенность данных:** (если обнаружена)
**Рабочий приём:** (приём кода, библиотека, подход)
**Грабли:** (на что наткнулся и как обойти)
**Оценка пользователя:** ✅ / без оценки

---

## Уроки

### [2026-08-09] Python 3.8: нет union types
**Тип задачи:** прочее (окружение)
**Что сделано:** разведочный скрипт упал на аннотации `def f(x) -> float | None`
**Особенность данных:** —
**Рабочий приём:** на Python 3.8 использовать `Optional[float]` из `typing`; синтаксис `X | None` доступен только с 3.10
**Грабли:** системный Python в окружении — 3.8.10 (2019 г.); любая современная аннотация/синтаксис (match, `|` в типах) не сработает
**Оценка пользователя:** без оценки

### [2026-08-09] regex: match ≠ search при поиске бренда в названии
**Тип задачи:** прочее (регэкспы)
**Что сделано:** извлечение бренда из названия («Молоко ЛЕБЕДЕВСКАЯ АФ пастер…»)
**Особенность данных:** названия начинаются со слова «Молоко», бренд — позже в строке
**Рабочий приём:** `re.search` (ищет по всей строке), а не `re.match` (только с начала); шаблон капс-группы `[А-ЯЁ]{2,}[0-9.]*(?:\s+[А-ЯЁ]{2,}[0-9.]*)*`; результат — `group(0)`
**Грабли:** `match` возвращал None, fallback `name.split()[0]` давал «Молоко» ВСЕМ товарам → сравнение брендов проходило всегда → мусорные пары. Проявлялось как «все бренды одинаковые»
**Оценка пользователя:** без оценки

### [2026-08-09] Бизнес-правило: шринкфляция = продолжение товара
**Тип задачи:** анализ
**Что сделано:** декомпозиция падения продаж
**Особенность данных:** товар, сменивший фасовку на меньшую (тот же бренд, та же жирность, зазор ≤ 90 дней), — это НЕ уход товара, а его продолжение. Склеивается в одну «историю»
**Рабочий приём:** скрипты 06 (поиск пар) + 07 (отчёт по историям); пары сохраняются в output/пары_шринкфляции.xlsx
**Грабли:** без склейки потери от «продолжившихся» товаров ошибочно записывались в «ушедшие»; после склейки видно, что именно они — главный источник падения (−50%)
**Оценка пользователя:** ✅ (правило задано пользователем)

**Уточнение пользователя (2026-08-09):** спорные пары (фасовка уменьшена, но жирность не совпала, напр. ДОМИК В ДЕРЕВНЕ 1400мл → 955г) — это РАЗНЫЕ товары, не склеивать. Склейка только при совпадении бренда И жирности.

### [2026-08-09] numpy.polyfit: порядок коэффициентов — от старшей степени к младшей
**Тип задачи:** модель (прогноз)
**Что сделано:** линейный тренд продаж по 18 месяцам (`np.polyfit(x, y, 1)`)
**Особенность данных:** —
**Рабочий приём:** для прямой `polyfit(x, y, 1)` возвращает `[наклон, свободный_член]` — распаковывать как `slope, inter`, а не `a, b` (иначе свободный член умножался на 18 месяцев → прогноз «взлетал» в 25 раз: 24 млн руб вместо 0,9 млн)
**Грабли:** ошибка «невидимая»: прогноз выглядел правдоподобным по структуре, но чудовищным по масштабу. Спасла грубая проверка на реальных данных: 3 дня августа-2026 экстраполировали на месяц и сравнили с прогнозом. Вывод: любой прогноз проверять на факте «последней точки»
**Оценка пользователя:** без оценки

### [2026-08-09] Требование пользователя: заголовки отчётов — по сути, не по коду
**Тип задачи:** прочее (оформление отчётов)
**Что сделано:** все книги отчётов переименованы на человеческие заголовки
**Рабочий приём:** «ТО, в е.изм.» → «Продано, штук»; «ТО руб» → «Выручка, руб»; «Старый_код» → «Код старой упаковки»; единица измерения — в скобках в заголовке. Заголовок должен объяснять сущность без заглядывания в код
**Грабли:** если переименовываются колонки в промежуточном файле (пары_шринкфляции.xlsx), надо синхронно обновить ВСЕ скрипты-читатели (06→07/08/09/10) — иначе KeyError
**Оценка пользователя:** ✅ (правило задано пользователем)

### [2026-08-09] pandas reindex: передача Series целиком вместо .values
**Тип задачи:** прочее (код)
**Что сделано:** добавление колонки «Товаров» (число товаров в день) в итоги по дням: `cnt.reindex(daily_tot["Дата"])`
**Особенность данных:** даты — объекты `datetime.date` в колонке object
**Рабочий приём:** надёжнее `daily_tot["Дата"].map(cnt)` или `reindex(daily_tot["Дата"].values)` — подстановка по значениям
**Грабли:** в изолированном тесте `reindex(Series)` работал, а внутри длинного скрипта вернул ВСЕ NaN при внешне идентичных датах (type/равенство/принадлежность set — True). Симптом — «тихая» пустая колонка без ошибки. Правило: для подстановки значений использовать `map`/`merge` по ключу, а не `reindex` с Series
**Оценка пользователя:** без оценки

### [2026-08-10] Калибровка прогноза: fillna(0.0) обнуляла товары без факта
**Тип задачи:** модель (баг)
**Что сделано:** сверка подневного прогноза августа с новым фактом (4–9) выявила товары с базой >0 и прогнозом 0
**Особенность данных:** 8 товаров не продавались 1–3 августа, но продавались в мае–июле
**Рабочий приём:** при расчёте множителя калибровки `ratio = факт / модель` НЕ делать `fillna(0.0)` до деления: 0/модель = 0 → прогноз всего месяца обнуляется. Оставлять NaN → `fillna(1.0)` = «как в мае–июле»
**Грабли:** тихий баг — прогноз правдоподобен, расхождение с фактом было всего +5,3% и не настораживало; сигнал — строки «прогноз 0, но продажи есть» в сверке. После исправления сверка 4–9 стала +0,9%. Вывод: сверка прогноза с фактом обязательна, а не опциональна
**Оценка пользователя:** без оценки

### [2026-08-10] Среднее за 3 месяца маскирует обвал в последнем месяце
**Тип задачи:** анализ (особенность данных)
**Что сделано:** разбор расхождения по МОЛОДЧИНКА 1л (прогноз 38,6 шт за 4–9, факт 0)
**Особенность данных:** апрель 24 → май 317 → июнь 263 → июль 18 → август 0 шт; товар фактически снят с продажи в июле, но база «среднее май–июль» (6,5 шт/день) этого не видит
**Рабочий приём:** для товаров с падающим трендом база «среднее за 3 месяца» завышает прогноз; контроль — посмотреть последний полный месяц отдельно (июль 18 шт = 0,6/день); при резком обвале стоит брать базу последнего месяца или взвешивать
**Грабли:** «средняя температура по больнице» скрывает конец тренда; сверка с фактом выявляет такие товары по строкам «прогноз был, продаж нет»
**Оценка пользователя:** без оценки

### [2026-08-11] Регистр токенов: «ОМСКИЙ».lower() != «ОМСКИЙ» — ложные пары
**Тип задачи:** код (поиск продолжений в колбасе)
**Что сделано:** мягкий поиск продолжений по бренду + отличительному слову
**Рабочий приём:** токены бренда сравнивать в едином регистре: `{t.lower() for t in brand.split()}` и `w.lower() not in …` — иначе «ОМСКИЙ» остаётся «отличительным» словом
**Грабли:** «Шпикачки ОМСКИЙ БЕКОН Домашние 400г» (изготовитель) ложно совпал с «Сардельки ОМСКИЙ БЕКОН мясные 400г» (в 2026 нет «Домашние» вообще): слово бренда в верхнем регистре прошло фильтр, т.к. сравнивалось lower-слово с верхним токеном. Симптом — «перекодировка» там, где товар реально исчез. После фикса: 1 лишняя пара отпала, 8 исчезнувших подтвердились все
**Оценка пользователя:** без оценки

### [2026-08-11] Маркетинговая приставка и длинное перекрытие: мягкий поиск продолжений
**Тип задачи:** анализ (поиск продолжений)
**Что сделано:** «Сервелат ТОРГОВАЯ ПЛОЩАДЬ Министерский 320г» → «… Как тогда Министерский 270г»
**Особенность данных:** приставка «Как тогда» сломала сравнение ядер названий; оба размера продавались параллельно больше года (первая продажа нового 2024-04-08 РАНЬШЕ последней старого 2025-05-02, зазор −389 дн — в обычный диапазон −60..90 не влезает)
**Рабочий приём:** второй проход — тот же бренд + ≥1 отличительное слово (≥6 букв, минус общие слова типа «сосиски/колбаса») в той же группе; диапазон зазора для параллельной продажи расширить до −480..730. Отличительные слова защищают от ложных пар внутри бренда (проверено: к «Домашние» в 2026 ничего не подходит)
**Грабли:** строгий поиск по ядру «потерял» живую позицию, и товар мог уйти в «исчез» — без второго прохода картина была бы неполной (9+9 вместо 10+8)
**Оценка пользователя:** без оценки

### [2026-08-11] Волны перефасовки бывают не только у частной марки: МИРАТОРГ сентябрь 2025
**Тип задачи:** анализ (поиск продолжений, база 2024)
**Что сделано:** «Колбаса МИРАТОРГ Балыковая в/к 375г» → «… Балыковая в/у 300г» (зазор −29 дн, перекрытие); «Краковская в/к 430г» → «… в/у 330г» (+12 дн)
**Особенность данных:** апрель-2024 волна перефасовки была у частной марки ТОРГОВАЯ ПЛОЩАДЬ, а в сентябре 2025 бренд МИРАТОРГ сменил упаковку в/к → в/у с уменьшением фасовки — те же признаки шринкфляции у поставщика-бренда
**Рабочий приём:** в core_name маркеры упаковки (в/у, в/к, п/к, с/у, бзмж) уже снимаются, поэтому склейка «в/к → в/у» сработала автоматически; при проверке продолжений спрашивать себя «а не перефасовка ли это другого бренда?», а не только частной марки
**Грабли:** если бы маркеры упаковки участвовали в ядре названия, обе пары МИРАТОРГ ушли бы в «исчез» — картина была бы ложной
**Оценка пользователя:** без оценки

### [2026-08-11] DataFrame.append удалён в pandas 2.0
**Тип задачи:** код (ABC-анализ)
**Что сделано:** добавление строки «ИТОГО» в сводку по группам
**Рабочий приём:** `pd.concat([df, tot_row.to_frame().T], ignore_index=True)`
**Грабли:** в pandas 2.0.3 `df.append(...)` больше не существует (AttributeError) — код из старых привычек (pandas 1.x) падает; замена — pd.concat. В этом проекте pandas 2.0.3
**Оценка пользователя:** без оценки

### [2026-08-11] «ТО, в е.изм.» — смешанные единицы: весовые (кг) и штучные (шт)
**Тип задачи:** данные (особенность файла, поправка пользователя)
**Что сделано:** пересборка ABC-анализа с разделением типов продажи
**Особенность данных:** колбасный файл содержит весовые товары (продажа резаная, дробные значения: 00814 «Любительская в синюге в/с 1кг» = 1,786 кг) и штучные (целое число упаковок: 210935 «Филейская с шпиком 350г» = 10 шт); колонка «ТО, в е.изм.» — это кг у одних и шт у других
**Рабочий приём (правило пользователя):** тип определяется по данным — есть хоть одна дробная продажа → весовой, кг; все продажи целые → штучный, шт. За год: 55 весовых / 213 штучных
**Грабли:** я дважды называл колонку «кг» и «штук в данных нет» — неверно; любые суммы/ранги «по количеству» без учёта типа смешивают несопоставимое (кг и шт в одном ранге). Отчёты 16/17 ранжировали топы смешанно — требуют пересчёта
**Оценка пользователя:** ✅ (поправка от пользователя)

### [2026-08-11] Генерация DOCX: postcheck требует Python 3.9+, страницы проверяю через LibreOffice
**Тип задачи:** код (отчёт для директора, формат docx)
**Что сделано:** краткий отчёт (1-2 стр.) через docx-библиотеку npm + пост-проверка skill'а
**Рабочие приёмы:**
- пост-проверка document-skills (`postcheck.py`) падает на Python 3.8 (`list[...]` в аннотациях) — запускать через `/opt/homebrew/bin/python3.13` (в системе есть 3.9.6/3.11/3.13)
- число страниц docx проверяю рендером: `soffice --headless --convert-to pdf --outdir /tmp/x file.docx` → ищу `/Type /Page` в PDF (pdftotext для просмотра содержимого страниц)
- межстрочный интервал задавать ОДИН раз в стиле документа (styles.default.document.paragraph.spacing.line=312), а не в каждом абзаце — иначе postcheck даёт warning «line-spacing inconsistent» (несовпадение явных и унаследованных значений)
- нумерованный список проверить в `word/numbering.xml`: должен быть `w:numFmt w:val="decimal"` с `w:lvlText w:val="%1."` (textutil показывает «•», это не показатель)
- требование «1-2 страницы» — цикл: ужать текст → перегенерировать → LibreOffice PDF → счётчик страниц, пока не сойдётся
- косая черта/«ё» и типографские символы «–», «→», «−» в JS-строках безопасны; кавычки-«ёлочки» («») безопасны, а вот «"…"» (curly) ломают JS
**Грабли:** первый вариант отчёта занял 3 страницы (свободные формулировки + длинные имена в таблице лидеров) — ужимал текст и ширины колонок, а не шрифт; с LibreOffice и Word пагинация близка, но при равных линиях Word обычно вмещает чуть больше
**Оценка пользователя:** без оценки

### [2026-08-11] Сверка двух версий отчёта: коды с ведущими нулями, полнота колонок
**Тип задачи:** код (пересчёт отчётов 16/17 с правильными единицами)
**Что сделано:** скрипт 21 пересчитал топы-2023/2024 с рангами в собственных единицах (весовой — кг, штучный — шт) и «≈ кг» для сравнения; старая версия не перезаписана (файлы _v2)
**Особенность данных:** код «02506» записывается в Excel как число «2506» — сравнивать коды версий нужно после нормализации `str.zfill(5)`, иначе ложное расхождение составов
**Рабочий приём:** тип продажи — по данным за весь период (есть хоть одна дробная продажа → весовой, кг), реюз проверенной логики продолжений из 16/17 без изменений; правильность нового файла проверяется программно по ТРЕМ осям: тип совпал, «Продано (шт/кг)» = сумме по исходнику, «≈ кг» = шт × фасовка/1000
**Грабли:** (1) при переносе логики из 16 в 21 я пропустил колонку «Последняя продажа» — KeyError на первом же запуске; поймано быстро, но правило: после портирования блока сверять список колонок вывода с оригиналом; (2) важный вывод: ранг в собственных единицах НЕ изменил составы звёзд (пересечение с топом по выручке фильтрует одинаково) — но сами цифры «кг» у штучных в 16/17 были завышены (45 068 «кг» vs честные 18 027 кг), так что пересчёт нужен был именно за правдой о числах
**Оценка пользователя:** без оценки

### [2026-08-12] Отраслевой отчёт: сводный JSON сверять с исходником, страницы считать pdfinfo
**Тип задачи:** отчёт с внешними данными (магазин ↔ отрасль, 7 групп)
**Что сделано:** отчёт 23 на 8 страниц; два фоновых исследовательских агента (WebSearch/WebFetch) + спот-проверка ключевых фактов через WebFetch напрямую (мелкие статьи, которые агент мог переврать) — обе проверки сошлись; store_data.json/industry_data.json как единые источники цифр
**Рабочий приём:** (1) магазин и отрасль имеют РАЗНЫЕ периоды (магазин янв–10 авг, отрасль I кв.) — в отчёте сравниваются направления и глубина динамики, а не темпы, с припиской об этом под таблицей; (2) macOS: soffice не в PATH — полный путь /Applications/LibreOffice.app/Contents/MacOS/soffice; число страниц — `pdfinfo` (regex `/Type /Page` по PDF не работает: LibreOffice пишет `/Type/Page` без пробела и страницы в сжатых объектах); (3) постпроверка docx — только python3.13 (postcheck требует 3.9+), а данные — python3 (3.8.10, где есть pandas)
**Грабли:** сводный JSON содержал перепутанные «кг» и «шт» (инвертировал фильтр весовой/штучный при одном из пересчётов) — расхождения всего 1–7 единиц, проценты и миллионы были верны, но публиковать нельзя; правило: перед сборкой отчёта сверять сводку с ПРЯМОЙ суммой по исходнику на эталонных числах (совпало со старым отчётом 20 — он оказался прав); ещё грабли: pdftotext рвёт строки, «1 065,9 млн» ищется по «1 065» — при проверке текста искать по фрагменту
**Оценка пользователя:** без оценки

### [2026-08-12] Сверка чужого файла-отчёта: восстановить правило рангов перебором, проверить единицы
**Тип задачи:** аудит внешнего файла (20260812_115818.xlsx) на соответствие заданию
**Что сделано:** сверил чужой отчёт с нашим (16) и с исходником: составы идентичны (18 кодов), критерии верны, никого не забыто (полнота = пересечение топ-10 по объёму и выручке в группе); итог — книга output/Сравнение_файлов_потерянных_топов.xlsx с вердиктом
**Особенность данных:** «ранг» — неоднозначное понятие: по всей группе или среди «кандидатов» (прошедших оба топ-10). В 6 строках чужого файла ранги «лучше» групповых — я восстановил его правило перебором гипотез (mixed / own-type / перекрёстно) и нашёл 18/18 совпадение на «ранги среди кандидатов». Правило: при аудите чужих файлов сначала подобрать методологию по полному перебору вариантов, потом судить
**Грабли:** (1) чужая шапка «шт» может содержать килограммы (весовая НПФ куриная 1кг = 4 993,6 «шт») — тип определять по дробности значений, а не верить шапке; (2) коды в чужом файле без ведущего нуля («2506») — нормализовать str.zfill(5) до сравнения; (3) проверять не только «что есть лишнее», но и полноту: «кандидаты без 2026» должны сойтись с числом строк файла по каждой группе (в сырокопчёной потерь 0 — группа в сводке отсутствует легитимно, но лучше показывать с нулём)
**Оценка пользователя:** без оценки

## [2026-08-19] HTML-дашборд с встроенными данными
- **Грабля**: JS-код внутри Python-тройной строки `"""..."""` — `\n` в JS (split('\n')) Python превращает в реальный перенос строки, браузер падает «Invalid or unexpected token». В исходнике пиши `\\n`.
- **Грабля**: переключение вкладок через `style.display = ''` НЕ показывает панель, если в CSS есть `.tab-panel{display:none}` — пустая строка снимает inline-стиль и возвращает CSS-правило. Ставь явно `'block'`/`'none'`.
- **Приём**: Chart.js в скрытых вкладках создаётся с размером 0×0 — строить графики лениво (при первом показе вкладки, через requestAnimationFrame).
- **Приём**: встраивание JSON в <script> — `json.dumps(ensure_ascii=False).replace("</", "<\\/")`; `<\/` валиден в JSON и не может закрыть тег раньше времени.
- **Приём**: проверка рендера file://-страницы — playwright-core из скила render + Chromium из ~/Library/Caches/ms-playwright; счётчик непрозрачных пикселей canvas (getImageData) подтверждает, что график реально нарисован.
